export const metadata = {
  title: 'Laska Legacy | WMG Quiz',
  description: 'SAWMGA trivia ladder — five levels, ten seconds a question.',
  robots: { index: false, follow: false },
};

export default function QuizLayout({ children }) {
  return children;
}
