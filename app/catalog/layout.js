export const metadata = {
  title: 'Laska Legacy | Catalog',
  description: 'Browse the full Laska Legacy range and order directly on WhatsApp.',
  robots: { index: false, follow: false },
};

export default function CatalogLayout({ children }) {
  return children;
}
